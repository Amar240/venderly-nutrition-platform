# Copy to .env and fill in. Never commit .env.

DATABASE_URL=postgresql://postgres:devpass@localhost:5432/woodbridge

# openssl rand -base64 32
AUTH_SECRET=
PAYMENT_SIM_SECRET=

# Low balance threshold in integer cents
LOW_BALANCE_THRESHOLD_CENTS=1000
